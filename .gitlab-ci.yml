# DinD service is required for Testcontainers
services:
  - docker:dind

variables:
  # Instruct Testcontainers to use the daemon of DinD.
  DOCKER_HOST: "tcp://docker:2375"
  # Improve performance with overlayfs.
  DOCKER_DRIVER: overlay2
  ANDROID_COMPILE_SDK: "29"
  ANDROID_BUILD_TOOLS: "29.0.3"
  ANDROID_SDK_TOOLS:   "6200805"

stages:
- build
- check-releasable
- sonar

build:compile-only:
  image: maven:3.6-openjdk-11
  stage: build
  when: manual
  script:
    - mvn clean package -Dmaven.test.skip
  tags:
    - docker

build:with-tests:
  image: maven:3.6-openjdk-11
  stage: build
  script:
    - mvn clean package
  except:
    - /^master.*$/
    - /^release\/.*$/
    - schedules
  tags:
    - docker

build:android-debug:
  image: registry.nuiton.org/codelutin/dockerfiles:debian-dev-java8
  stage: build
  when: manual
  script:
    - apt-get --quiet update --yes
    - apt-get --quiet install --yes wget tar unzip lib32stdc++6 lib32z1
    - wget --output-document=android-sdk.zip https://dl.google.com/android/repository/commandlinetools-linux-${ANDROID_SDK_TOOLS}_latest.zip
    - unzip -d android-sdk-linux android-sdk.zip
    - export ANDROID_HOME=$PWD/android-sdk-linux
    - echo y | android-sdk-linux/tools/bin/sdkmanager --sdk_root=${ANDROID_HOME} "platforms;android-${ANDROID_COMPILE_SDK}" >/dev/null
    - echo y | android-sdk-linux/tools/bin/sdkmanager --sdk_root=${ANDROID_HOME} "platform-tools" >/dev/null
    - echo y | android-sdk-linux/tools/bin/sdkmanager --sdk_root=${ANDROID_HOME} "build-tools;${ANDROID_BUILD_TOOLS}" >/dev/null
    - export PATH=$PATH:$PWD/android-sdk-linux/platform-tools/
    - chmod +x fishola-mobile/android/gradlew
    # temporarily disable checking for EPIPE error and use yes to accept all licenses
    - set +o pipefail
    - yes | android-sdk-linux/tools/bin/sdkmanager --sdk_root=${ANDROID_HOME} --licenses
    - set -o pipefail
    - cd fishola-mobile
    - npm install
    - cd android
    - ./gradlew assembleDebug
  artifacts:
    paths:
      - fishola-mobile/android/app/build/outputs/
  tags:
    - android

build:ios-debug:
  stage: build
  when: manual
  script:
    - cd fishola-mobile/ios/App
    - xcodebuild clean -project App.xcodeproj -scheme App | xcpretty
    - pod install
    - xcodebuild -scheme 
  #  - xcodebuild clean archive -archivePath build/App -scheme App
  #  - xcodebuild -exportArchive -exportFormat ipa -archivePath "build/App.xcarchive" -exportPath "build/App.ipa" -exportProvisioningProfile "fr.inrae.fishola"
  tags:
    - ios

#check-releasable:
#  image: registry.nuiton.org/codelutin/dockerfiles:maven-release-debian9-openjdk8-with-frequent-libraries
#  stage: check-releasable
#  script:
#    - mvn clean verify -e -U -DperformRelease -Dredmine.skipGenerateChanges -Dmaven.javadoc.skip
#    - rm * .git* -rf || true
#  only:
#    - /^develop.*$/
#  when: manual
#  tags:
#    - tmpfs
#
#daily-check-releasable:
#  image: registry.nuiton.org/codelutin/dockerfiles:maven-release-debian9-openjdk8-with-frequent-libraries
#  stage: check-releasable
#  script:
#    - mvn clean verify -e -U -DperformRelease -Dredmine.skipGenerateChanges -Dmaven.javadoc.skip
#    - rm * .git* -rf || true
#  only:
#    - schedules
#  tags:
#    - tmpfs
#
#sonar_job:
#  image: registry.nuiton.org/codelutin/dockerfiles:maven-release-debian9-openjdk8-with-frequent-libraries
#  stage: sonar
#  script:
#    - mvn -e -U clean verify -Dmaven.test.skip -DskipRelease org.sonarsource.scanner.maven:sonar-maven-plugin:3.4.0.905:sonar -Dsonar.host.url=https://qa.codelutin.com -Dsonar.projectKey=fr.afb:sispea -Dsonar.exclusions='**/js/libs/*','**/target/*','**/generated*/*'
#    - rm * .git* -rf || true
#  only:
#    - schedules
#  tags:
#    - tmpfs
