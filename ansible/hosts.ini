# Le déploiement se fait depuis une session déjà connectée en SSH sur le
# serveur dev (cf. DEPLOYMENT.md) : Ansible tourne donc en local sur cette
# machine, pas de saut SSH à configurer ici.
[dev]
localhost ansible_connection=local
