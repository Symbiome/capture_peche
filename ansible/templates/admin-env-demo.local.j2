# Généré par ansible/playbooks/deploy.yml depuis API_DOMAIN (.env) — ne pas
# éditer à la main, régénéré à chaque déploiement.
NODE_ENV=production
DIST_FOLDER=demo
VITE__API_URL=https://{{ api_domain }}/api
VITE__ENV_NAME=dev
